version: '2.1'
services:
  trilium:
    container_name: trilium
    image: zadam/trilium
    restart: always
    ports:
      - "3388:8080"
    volumes:
      - /portainer/Files/AppData/Config/trilium:/home/node/trilium-data
